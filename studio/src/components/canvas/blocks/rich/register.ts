// studio/src/components/canvas/blocks/rich/register.ts — STUB (lane A, 11.1).
// Lane D replaces the body with registerBlock() calls for draft, compass and
// frame. The exported signature stays. Until then the fallback renderer shows.

export function registerRichBlocks(): void {
  // no-op: FallbackBlock renders draft, compass and frame
}

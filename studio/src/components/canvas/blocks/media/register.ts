// studio/src/components/canvas/blocks/media/register.ts — STUB (lane A, 11.1).
// Lane E replaces the body with registerBlock() calls for image, gallery,
// recording and palette. The exported signature stays. Until then the fallback
// renderer shows.

export function registerMediaBlocks(): void {
  // no-op: FallbackBlock renders the four media types
}

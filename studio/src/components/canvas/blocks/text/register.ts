// studio/src/components/canvas/blocks/text/register.ts — STUB (lane A, 11.1).
// Lane C replaces the body with registerBlock() calls for the ten text types
// (concept, since, update, timeline, anchor, note, heading, divider, reference,
// commitment). The exported signature stays. Until then the fallback renderer shows.

export function registerTextBlocks(): void {
  // no-op: FallbackBlock renders every text type
}
